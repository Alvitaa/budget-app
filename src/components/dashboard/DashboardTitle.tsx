import { useState } from "react";

type DashboardTitleProps = {
    months: string[];
    years: string[];
}


// TODO: Hacer que en vez de select se cambie por un botón que abra el calendario.
export default function DashboardTitle() {
    return (
        <h1>
            Dashboard
            
        </h1>
    );
}